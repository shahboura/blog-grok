---
import { Image } from 'astro:assets';
import BaseLayout from './BaseLayout.astro';
import FormattedDate from '../components/FormattedDate.astro';
import { config } from '../config';
import DifficultyBadge from '../components/DifficultyBadge.astro';
import CategoryBadge from '../components/CategoryBadge.astro';
import { copy, checkmark, close } from 'ionicons/icons';
import getReadingTime from 'reading-time';
import { toString } from 'mdast-util-to-string';

const { title, description, pubDate, updatedDate, heroImage, tags, category, series, part, difficulty, excerpt, minutesRead: readingTime } = Astro.props as any;

// Calculate reading time using the reading-time library (following Astro docs approach)
let minutesRead = readingTime; // Use from frontmatter if available

// If no reading time in frontmatter, calculate it from the excerpt (available data)
if (!minutesRead && excerpt) {
    const readingTimeResult = getReadingTime(excerpt);
    minutesRead = readingTimeResult.text;
}

// Generate schema.org structured data
const canonicalUrl = new URL(Astro.url.pathname, Astro.site);
const schemaData = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  "headline": title,
  "description": description,
  "image": heroImage ? new URL(heroImage.src, Astro.url).toString() : undefined,
  "datePublished": pubDate.toISOString(),
  "dateModified": updatedDate ? updatedDate.toISOString() : pubDate.toISOString(),
  "author": {
    "@type": "Person",
    "name": config.author.name,
    "url": new URL(config.author.url, Astro.site || 'https://example.com').toString()
  },
  "publisher": {
    "@type": "Organization",
    "name": config.title,
    "url": Astro.site?.toString() || 'https://example.com'
  },
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": canonicalUrl.toString()
  },
  "articleSection": category,
  "keywords": tags ? tags.join(', ') : undefined,
  "url": canonicalUrl.toString()
};
---

<BaseLayout
	title={title}
	description={description}
	image={heroImage}
	structuredData={schemaData}
	article={{
		publishedTime: pubDate,
		modifiedTime: updatedDate,
		author: config.author.name,
		section: category,
		tags: tags
	}}
>
	<article>
		<div class="hero-image">
			{heroImage && <Image width={1020} height={510} src={heroImage} alt={title} />}
		</div>
		<div class="prose">
			<div class="title">
				<div class="date">
					<FormattedDate date={pubDate} />
					{
						updatedDate && (
							<div class="last-updated-on">
								Last updated on <FormattedDate date={updatedDate} />
							</div>
						)
					}
				</div>
				<h1>{title}</h1>
				<hr />
			</div>

			{/* Post metadata */}
			<div class="post-meta">
				<div class="meta-row">
					{difficulty && <DifficultyBadge level={difficulty} />}
					{minutesRead && <span class="reading-time">{minutesRead}</span>}
					{category && <CategoryBadge category={category} />}
				</div>

				{series && (
					<div class="series-info">
						<span class="series-label">📚 Series: {series}</span>
						{part && <span class="part-label">Part {part}</span>}
					</div>
				)}

				{excerpt && (
					<div class="excerpt">
						<p>{excerpt}</p>
					</div>
				)}
			</div>

			{/* Series navigation */}
			{series && (
				<div class="series-navigation">
					<a href={`/blog/series/${series.toLowerCase().replace(/\s+/g, '-')}`} class="series-link">
						← View Series Overview
					</a>
				</div>
			)}

			<slot />
		</div>
	</article>

 	<script>
 		document.addEventListener('DOMContentLoaded', () => {
 			document.querySelectorAll('pre').forEach(pre => {
 				const button = document.createElement('button');
  				button.innerHTML = `
  					<ion-icon name="copy" size="small"></ion-icon>
  					<span>Copy</span>
  				`;
 				button.className = 'copy-code-btn';
 				button.setAttribute('aria-label', 'Copy code to clipboard');
 				button.setAttribute('title', 'Copy code to clipboard');

 				let timeoutId: any;

 				const originalHTML = button.innerHTML;

 				button.addEventListener('click', async () => {
 					const code = pre.querySelector('code');
 					if (code && navigator.clipboard) {
 						try {
 							await navigator.clipboard.writeText(code.textContent || '');
  							button.innerHTML = '<ion-icon name="checkmark" size="small"></ion-icon><span>Copied!</span>';
 							button.classList.add('copied');
 						} catch (err) {
 							console.error('Copy failed:', err);
  							button.innerHTML = '<ion-icon name="close" size="small"></ion-icon><span>Failed</span>';
 							button.classList.add('error');
 						}

 						clearTimeout(timeoutId);
 						timeoutId = setTimeout(() => {
 							button.innerHTML = originalHTML;
 							button.classList.remove('copied', 'error');
 						}, 2000);
 					}
 				});

 				pre.style.position = 'relative';
 				pre.appendChild(button);
 			});
 		});
 	</script>

	<style>
		.hero-image {
			width: 100%;
		}
		.hero-image img {
			display: block;
			margin: 0 auto;
			border-radius: 12px;
			box-shadow: var(--box-shadow);
		}

		/* Copy code button styling */
		.copy-code-btn {
			position: absolute;
			top: 12px;
			right: 12px;
			display: flex;
			align-items: center;
			gap: 6px;
			padding: 6px 10px;
			font-size: 12px;
			font-weight: 500;
			background: rgba(255, 255, 255, 0.9);
			color: rgb(var(--gray-dark));
			border: 1px solid rgb(var(--gray));
			border-radius: 6px;
			cursor: pointer;
			transition: all 0.2s ease;
			backdrop-filter: blur(8px);
			-webkit-backdrop-filter: blur(8px);
			z-index: 10;
		}

		.copy-code-btn:hover {
			background: rgba(255, 255, 255, 0.95);
			border-color: rgb(var(--accent));
			color: rgb(var(--accent));
			transform: translateY(-1px);
		}

		.copy-code-btn:active {
			transform: translateY(0);
		}

		.copy-code-btn ion-icon {
			flex-shrink: 0;
			transition: all 0.2s ease;
		}

		.copy-code-btn.copied {
			background: rgba(34, 197, 94, 0.1);
			border-color: rgb(34, 197, 94);
			color: rgb(34, 197, 94);
		}

		.copy-code-btn.copied ion-icon {
			color: rgb(34, 197, 94);
		}

		.copy-code-btn.error {
			background: rgba(239, 68, 68, 0.1);
			border-color: rgb(239, 68, 68);
			color: rgb(239, 68, 68);
		}

		.copy-code-btn.error ion-icon {
			color: rgb(239, 68, 68);
		}

		/* Dark theme support */
		[data-theme="dark"] .copy-code-btn {
			background: rgba(30, 41, 59, 0.9);
			color: rgb(var(--gray-light));
			border-color: rgb(var(--gray));
		}

		[data-theme="dark"] .copy-code-btn:hover {
			background: rgba(30, 41, 59, 0.95);
			color: rgb(var(--accent));
		}

		[data-theme="dark"] .copy-code-btn.copied {
			background: rgba(34, 197, 94, 0.15);
		}

		[data-theme="dark"] .copy-code-btn.error {
			background: rgba(239, 68, 68, 0.15);
		}
		.prose {
			width: 720px;
			max-width: calc(100% - 2em);
			margin: auto;
			padding: 1em;
			color: rgb(var(--gray-dark));
		}
		.title {
			margin-bottom: 1em;
			padding: 1em 0;
			text-align: center;
			line-height: 1;
		}
		.title h1 {
			margin: 0 0 0.5em 0;
		}
		.date {
			margin-bottom: 0.5em;
			color: rgb(var(--gray));
		}
		.last-updated-on {
			font-style: italic;
		}
		.post-meta {
			margin: 1.5em 0;
			padding: 1em;
			background: rgb(var(--gray-light));
			border-radius: 8px;
			border-left: 4px solid rgb(var(--accent));
		}
		[data-theme="dark"] .post-meta {
			background: rgb(var(--gray-dark));
		}
		.meta-row {
			display: flex;
			flex-wrap: wrap;
			gap: 1em;
			align-items: center;
			margin-bottom: 0.5em;
		}
		.reading-time {
			color: rgb(var(--gray));
			font-size: 0.9em;
		}
		.series-info {
			margin-top: 0.5em;
			padding-top: 0.5em;
			border-top: 1px solid rgb(var(--gray));
			display: flex;
			justify-content: space-between;
			align-items: center;
		}
		.series-label {
			font-weight: 600;
			color: rgb(var(--accent));
		}
		.part-label {
			background: rgb(var(--accent));
			color: white;
			padding: 0.25em 0.5em;
			border-radius: 4px;
			font-size: 0.875em;
			font-weight: 600;
		}
		.excerpt {
			margin-top: 1em;
			font-size: 1.1em;
			line-height: 1.6;
			font-style: italic;
			color: rgb(var(--gray-dark));
		}
		.series-navigation {
			margin: 2em 0;
			text-align: center;
		}
		.series-link {
			display: inline-block;
			padding: 0.75em 1.5em;
			background: rgb(var(--accent));
			color: white;
			text-decoration: none;
			border-radius: 6px;
			font-weight: 500;
			transition: background 0.2s;
		}
		.series-link:hover {
			background: rgb(var(--accent-dark));
		}

	</style>
</BaseLayout>
